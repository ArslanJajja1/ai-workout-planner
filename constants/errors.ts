// Shared API error messages for consistency across handlers.
export const MISSING_KEY_ERROR = "OPENAI_API_KEY is not configured on the server.";
export const INVALID_JSON_ERROR = "Request body must be valid JSON.";
export const DEFAULT_PLAN_ERROR = "Something went wrong while generating the plan.";

// Default error message for all API errors.
export const DEFAULT_ERROR_MESSAGE = "Something went wrong while generating your plan.";
