/**
 * Simple loading placeholder while the plan hydrates from storage.
 */
export function PlanLoadingState() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-gray-50">
      <p className="text-sm font-medium text-gray-500">Preparing your workout plan…</p>
    </main>
  );
}


